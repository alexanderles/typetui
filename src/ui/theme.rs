//! Shared theme colors for all screens.

use ratatui::style::Color;

/// Primary theme color used for highlights, accents, and results headline values.
pub const PRIMARY: Color = Color::Cyan;
/// Muted gray for labels and axes.
pub const LABEL: Color = Color::Rgb(100, 102, 105);
